{
  "name": "view-source-switch",
  "version": "2.0.0",
  "description": "One-click switch between web page and view-source",
  "main": "background.js",
  "scripts": {
    "pretest": "npx tsc",
    "test": "npx web-ext lint --ignore-files=tsconfig.json package.json package-lock.json images README.md */*.ts */*.drawio",
    "prebuild": "npx tsc",
    "build": "npx web-ext build --ignore-files=tsconfig.json package.json package-lock.json images README.md */*.ts */*.drawio --overwrite-dest",
    "preserve": "npx tsc",
    "serve": "npx web-ext run",
    "check-lib": "npm outdated"
  },
  "author": "Florent Tassy",
  "license": "GPL-3.0-or-later",
  "dependencies": {},
  "homepage": "https://github.com/ftassy/ViewSourceSwitch",
  "devDependencies": {
    "typescript": "^4.1.2",
    "web-ext": "^5.4.0"
  }
}
